---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span
  class={`rounded-full border border-border bg-surface-2 px-3 py-1 font-mono text-sm text-muted ${className}`}
>
  <slot />
</span>
