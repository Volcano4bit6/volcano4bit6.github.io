---
interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className = '', hover = false } = Astro.props;
---

<div class={`card ${hover ? 'card-hover' : ''} ${className}`}>
  <slot />
</div>
